# LockBox 🔐
A secure file sharing system built with React (frontend) and Node.js + Express + MongoDB (backend).

## Setup
### Frontend
cd frontend
npm install
npm run dev

### Backend
cd backend
npm install
npm start
